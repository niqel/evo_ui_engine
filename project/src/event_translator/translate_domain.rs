use crate::event_interpreter::interpret_codomain::InternalEvent;

/// Dominio del actor EventTranslator.
/// Representa un evento funcional interno, aún no traducido.
pub type TranslateDomain = InternalEvent;
