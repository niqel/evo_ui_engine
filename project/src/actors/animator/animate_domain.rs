/// Dominio del actor Animator: la escena/estado actual antes del tick/evento.
/// Por ahora usamos la misma Scene que consume el SnapshotBuilder.
pub type AnimateDomain = crate::core::TranslateDomain;
