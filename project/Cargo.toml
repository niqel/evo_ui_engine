[package]
name = "evo_ui_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
vello   = { version = "0.5", default-features = false, features = ["wgpu"] }
kurbo   = { version = "0.11", features = ["std"] }
pollster = "0.4"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
winit = "0.30"

[dev-dependencies]
png = "0.17"


# ❌ elimina estas secciones si las tienes
# wgpu = { version = "0.19", optional = true }
# [features]
# default = []
# vello = ["dep:wgpu"]
